PLAYER COUNT LIVE-SITE FIX

The project is currently set to 4 players.

To publish it:
1. Double-click setup_powershell\Deploy Firestore Rules.cmd
2. Double-click setup_powershell\Update Entire Project to GitHub.cmd
3. Wait for GitHub Pages to finish, then press Ctrl+F5 on the website.

The Change Player Count tool now changes the multiplayer-config.js URL every time, preventing the browser from reusing an older cached player count.
